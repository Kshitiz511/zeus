"use client";

/**
 * PinnedStoryScene — Now merged into HeroScene as one continuous scroll experience.
 * This component is kept as a no-op export to avoid breaking imports.
 */
export function PinnedStoryScene() {
  return null;
}
